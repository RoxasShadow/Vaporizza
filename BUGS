- If you set a largest cover size, the gadget could moves out of screen. So just move the gadget to the left side of screen and then resize.
- After three or four switches between 'show' and 'hide' non-Steam games, the setting could be ignored. So just restart the gadget.